#include<stdio.h>

int main(){
	int length = 5;
	int breadth = 7;
	int area = length * breadth;
	printf( "The length is %d and then breadth is %d so the area : %d\n", length, breadth, area);
	return 0;
}
